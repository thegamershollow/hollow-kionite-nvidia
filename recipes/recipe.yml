---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: hollow-kionite-nvidia
description: A custom fedora kionite build meant for my gaming pc
base-image: ghcr.io/ublue-os/kinoite-nvidia
image-version: 43

platforms:
  - linux/amd64

modules:
  - type: files
    files:
      - source: system
        destination: /

  - type: dnf
    repos:
      files:
        - https://pkgs.tailscale.com/stable/fedora/tailscale.repo

    install:
      packages:
        - fzf
        - zsh
        - ruby
        - ruby-devel
        - hyfetch
        - fastfetch
        - tailscale
        - mediawriter
        - cpu-x
        - kcalc
        - kcharselect
        - ksystemlog
        - kdf
        - spectacle
        - okular
        - gwenview
        - gnome-disk-utility

    remove:
      packages:
        - firefox
        - firefox-langpacks
        - kde-partitionmanager

  - type: script
    snippets:
      - sudo dnf5 install -y https://dl.google.com/linux/direct/google-chrome-stable_current_x86_64.rpm
      - sudo dnf5 install @development-tools && sudo gem install colorls && sudo dnf5 remove @development-tools
      - sudo systemctl enable tailscaled.service
      - sudo dnf5 install https://mirrors.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm && sudo dnf5 install steam discord

  - type: default-flatpaks
    configurations:
      - notify: false
        scope: system

        remotes:
          - name: flathub
            url: https://dl.flathub.org/repo/flathub.flatpakrepo

          - name: fedora
            url: https://registry.fedoraproject.org
            type: oci
            gpg-verify: false

        install:
          - com.github.tchx84.Flatseal
          - it.mijorus.gearlever
          - com.usebottles.bottles
          - org.kde.kweather
          - org.kde.kate
          - org.kde.okular
          - org.kde.gwenview
          - org.kde.spectacle

  - type: signing
